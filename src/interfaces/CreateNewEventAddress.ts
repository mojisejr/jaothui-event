export interface CreateNewEventAddressDTO {
  userId: string;
  eventId: string;
  eventRegisterId: string;
  address: string;
  district: string;
  amphoe: string;
  province: string;
  zipcode: string;
}
